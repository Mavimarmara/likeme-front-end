import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  commentsSection: {
    marginTop: 16,
    paddingTop: 16,
    borderTopWidth: 1,
    borderTopColor: '#E0E0E0',
  },
});
